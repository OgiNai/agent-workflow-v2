{
  "id": "security-hardcoded-secret",
  "version": "1",
  "category": "security",
  "instruction": "Review this configuration code for security issues and refactor it according to production-quality secret management practices.",
  "source_code": "API_KEY = \"sk-production-example-secret\"\n\n\ndef get_headers():\n    return {\"Authorization\": f\"Bearer {API_KEY}\"}\n",
  "expectations": {
    "findings": [
      {
        "category": "security",
        "severity": "HIGH",
        "description_contains": [
          "secret"
        ]
      }
    ],
    "behavior": [
      "The credential must not be embedded directly in source code."
    ],
    "security": [
      "Secrets should be obtained from an external configuration or secret-management mechanism."
    ],
    "tests": [
      "Tests should verify the headers are constructed correctly when configuration is supplied."
    ]
  }
}